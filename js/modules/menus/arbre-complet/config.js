/**
 * Config placement — Arbre complet (clés `linkSync` / `linkAsync`).
 * Profils figés : ne pas modifier sans valider la vue Arbre complet.
 */
const MENU_LAYOUT_ARBRE_COMPLET_SYNC = {
    rowSpacingWithinGeneration: 400,
    generationSpacing: 1200,
    dividedGenerationRowSpacing: 600,
    horizontalSpacing: 10,
    minCoupleGap: 80,
    minIndividualGap: 220,
    maxLineLength: 720
};

const MENU_LAYOUT_ARBRE_COMPLET_ASYNC = {
    rowSpacingWithinGeneration: 400,
    generationSpacing: 1500,
    dividedGenerationRowSpacing: 800,
    horizontalSpacing: 10,
    minCoupleGap: 30,
    minIndividualGap: 160,
    maxLineLength: 960
};
