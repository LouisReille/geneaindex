/**
 * Config placement — Nom de famille (clé `family`).
 * Le graphe utilise le placeur « arbre complet » avec fusion de ce profil.
 */
const MENU_LAYOUT_NOM_DE_FAMILLE = {
    rowSpacingWithinGeneration: 400,
    generationSpacing: 1200,
    dividedGenerationRowSpacing: 600,
    horizontalSpacing: 36,
    minCoupleGap: 110,
    minIndividualGap: 380,
    maxLineLength: 720
};
